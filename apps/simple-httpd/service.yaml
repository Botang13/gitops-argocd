apiVersion: v1
kind: Service
metadata:
  name: simple-httpd
  namespace: default
spec:
  type: NodePort
  selector:
    app: simple-httpd
  ports:
  - name: http
    port: 80
    targetPort: 80
    nodePort: 30081